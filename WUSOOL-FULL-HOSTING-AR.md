# نشر منصة وصول كاملة من جهازك على Railway أو Render

## النتيجة المختصرة

نعم، توجد منصات أنسب من Vercel لتشغيل النسخة الكاملة الحالية. أوصي بـ **Railway** لأنه يشغّل تطبيق Node.js طويل التشغيل مع الخادم وقاعدة البيانات بسهولة أكبر. ويمكن استخدام **Render** كخيار بديل.

منصة وصول الحالية تتكون من واجهة React وخادم Express وtRPC وقاعدة بيانات MySQL أو TiDB وتخزين خارجي للملفات. لذلك يجب نشرها كخدمة Web Service كاملة، وليس كموقع ثابت.

توضح وثائق Render الرسمية أن تطبيق Express يُنشر كـWeb Service مع أمر بناء وأمر تشغيل. وتوضح وثائق Railway أن النشر يمكن أن يتم من GitHub، مع إضافة متغيرات البيئة وإنشاء نطاق عام.[1] [2]

## الخيار الموصى به: Railway

### المتطلبات

تحتاج إلى كمبيوتر، وحساب GitHub، وحساب Railway، ونسخة المشروع الآمنة. لا تستخدم نسخة تحتوي على ملف إعدادات محلي أو مفاتيح قاعدة بيانات.

### 1. تجهيز المشروع على جهازك

فك ضغط نسخة المشروع. افتح المجلد الذي يحتوي على `package.json`، ثم افتح Terminal أو PowerShell داخله.

ثبّت المكتبات واختبر المشروع:

```text
pnpm install
pnpm check
pnpm test
pnpm build
```

إذا نجحت الأوامر، فالمشروع جاهز للرفع إلى GitHub.

### 2. إنشاء مستودع خاص في GitHub

أنشئ مستودعًا جديدًا باسم مثل `wusool-106`. اجعله **Private** لأن المنصة تحتوي على إعدادات خاصة بالمدرسة.

من داخل مجلد المشروع نفّذ:

```text
git init
git add .
git commit -m "Wusool class 106 portal"
git branch -M main
git remote add origin رابط-مستودعك
 git push -u origin main
```

قبل الرفع، تأكد من عدم وجود أي ملف يحتوي على أسرار. لا ترفع ملفات `.env` أو `.project-config.json` أو أي ملف يحتوي على رابط قاعدة البيانات أو مفاتيح التخزين.

### 3. إنشاء مشروع Railway

ادخل إلى [Railway](https://railway.com)، ثم سجّل الدخول بواسطة GitHub.

اختر **New Project**، ثم اختر **Deploy from GitHub repo**. حدد مستودع `wusool-106`.

ستنشئ Railway خدمة للتطبيق. لا تختَر موقعًا ثابتًا، بل اختر خدمة تشغّل Node.js.

### 4. أوامر البناء والتشغيل

من إعدادات خدمة التطبيق استخدم:

- أمر البناء: `pnpm install && pnpm build`
- أمر التشغيل: `pnpm start`

إذا طلبت Railway تحديد نسخة Node.js، استخدم Node.js 22 أو نسخة LTS حديثة متوافقة مع المشروع.

### 5. إضافة متغيرات البيئة

من خدمة التطبيق افتح قسم **Variables**، ثم أضف المتغيرات التالية بالقيم الخاصة بمشروعك:

- `DATABASE_URL`
- `JWT_SECRET`
- `BUILT_IN_FORGE_API_URL`
- `BUILT_IN_FORGE_API_KEY`
- `VITE_FRONTEND_FORGE_API_URL`
- `VITE_FRONTEND_FORGE_API_KEY`
- `VITE_OAUTH_PORTAL_URL`
- `VITE_APP_ID`
- `OAUTH_SERVER_URL`
- `OWNER_OPEN_ID`
- `OWNER_NAME`

لا تضع القيم الحقيقية داخل هذه الوثيقة أو داخل GitHub. أضفها من لوحة Railway فقط.

إذا كان حساب المدير الخاص بالمدرسة يعتمد على قاعدة البيانات الحالية، يجب استخدام نفس `DATABASE_URL` التي تحتوي على بيانات شعبة 106. لا تنشئ قاعدة جديدة فارغة إلا إذا كنت تريد بدء المشروع من الصفر.

### 6. التأكد من قاعدة البيانات

قاعدة البيانات الحالية MySQL أو TiDB. تأكد من أن Railway يستطيع الوصول إليها من خلال الإنترنت، وأن الاتصال يستخدم SSL إذا كانت الخدمة تتطلب ذلك.

لا تحذف الجداول الحالية. يجب أن تبقى جداول المواد والدروس والواجبات والاختبارات والبحوث والجدول وسجل التغييرات موجودة.

### 7. إنشاء رابط عام

بعد نجاح عملية البناء والتشغيل، افتح إعدادات خدمة التطبيق في Railway، ثم افتح قسم **Networking**، واختر **Generate Domain**.

سيظهر رابط عام للموقع. افتحه في المتصفح واختبر صفحة الطلاب وصفحة الإدارة.

### 8. اختبار المزامنة بين جهازين

اختبر الموقع بهذه الطريقة:

1. افتح رابط Railway من الجهاز الأول.
2. افتح صفحة الإدارة وسجّل الدخول.
3. أضف واجبًا تجريبيًا لشعبة 106.
4. افتح الرابط من جوال أو جهاز ثانٍ.
5. تأكد من ظهور الواجب في صفحة الطلاب.
6. أضف رابطًا أو مرفقًا تجريبيًا.
7. عدّل المرفق أو احذفه.
8. افتح سجل التغييرات من لوحة الإدارة.
9. تأكد من ظهور اسم المدير والعملية والتاريخ.

إذا ظهرت البيانات على الجهاز الأول فقط، فالمشكلة غالبًا أن `DATABASE_URL` غير صحيحة أو أن التطبيق لا يستطيع الوصول إلى قاعدة البيانات.

## خيار بديل: Render

Render مناسب أيضًا لتشغيل خدمة Node.js كاملة.

### خطوات Render

1. أنشئ مستودعًا خاصًا في GitHub.
2. ادخل إلى [Render](https://render.com).
3. اختر **New** ثم **Web Service**.
4. اربط حساب GitHub واختر مستودع وصول.
5. اختر لغة Node.
6. استخدم أمر البناء: `pnpm install && pnpm build`.
7. استخدم أمر التشغيل: `pnpm start`.
8. أضف متغيرات البيئة نفسها المذكورة في قسم Railway.
9. اضغط **Create Web Service**.
10. انتظر انتهاء البناء ثم افتح رابط `onrender.com`.

Render ينشر تحديثًا جديدًا تلقائيًا عند دفع تغييرات جديدة إلى الفرع المتصل بالمشروع.[1]

## ما يجب عدم فعله

لا ترفع مجلد `client` وحده إلى المنصة، لأنه يحتوي على الواجهة فقط.

لا تستخدم Vercel أو أي استضافة ثابتة إذا كان الهدف تشغيل الإدارة والمزامنة ورفع الملفات دون تجهيز الخادم لها.

لا تضع كلمة مرور المدير داخل ملفات الواجهة أو داخل GitHub.

لا تشارك `DATABASE_URL` أو `JWT_SECRET` أو مفاتيح التخزين في الصور أو المحادثات.

لا تستخدم قاعدة بيانات جديدة بدل قاعدة شعبة 106 دون أخذ نسخة احتياطية ومعرفة أثر ذلك على البيانات الحالية.

لا تعلن الرابط للمدرسة كاملة قبل التأكد من عزل بيانات شعبة 106، لأن المرحلة الحالية مخصصة لهذه الشعبة فقط.

## التكاليف والاستمرارية

يجب مراجعة خطة المنصة قبل اعتمادها للمدرسة كاملة. بعض الخطط المجانية قد توقف الخدمة عند عدم الاستخدام أو تضع حدودًا على الموارد. تشغيل قاعدة البيانات والتخزين ورفع الملفات قد يحتاج إلى خطة مدفوعة حسب الاستخدام.

للاستخدام التجريبي لشعبة 106، ابدأ بأقل إعداد مناسب وراقب السجلات. قبل الاستخدام الرسمي على مستوى المدرسة، استخدم خطة تضمن تشغيلًا مستمرًا ونسخًا احتياطية وقاعدة بيانات مستقرة.

## خطة التوسع من 106 إلى المدرسة

ابدأ بشعبة 106 فقط. بعد التأكد من نجاح المزامنة والصلاحيات، يجب إضافة الصف والشعبة إلى تنظيم البيانات حتى يرى كل طالب محتوى شعبته فقط.

بعد ذلك يمكن إضافة حسابات المعلمين والطلاب وأولياء الأمور، ثم إنشاء لوحة إدارة مركزية للمدرسة. لا تنقل النظام إلى المدرسة كاملة بمجرد نجاح الصفحة؛ يجب أولًا اختبار عزل البيانات والملفات والصلاحيات.

## حل الأخطاء الشائعة

### الموقع يفتح لكن صفحة الإدارة لا تعمل

تحقق من وجود `DATABASE_URL` و`JWT_SECRET` في متغيرات البيئة. ثم راجع سجلات الخدمة وابحث عن أخطاء الاتصال بقاعدة البيانات.

### الموقع يعرض صفحة فارغة

راجع أمر البناء، وتأكد من تشغيل `pnpm build` بنجاح. ثم تأكد من أن أمر التشغيل هو `pnpm start` وليس أمر التطوير.

### الإضافة لا تظهر على جهاز آخر

تحقق من أن الجهازين يستخدمان نفس رابط النشر، وأن `DATABASE_URL` تشير إلى قاعدة البيانات نفسها. لا تعتمد على التخزين المحلي للمتصفح في البيانات المدرسية.

### الملف المرفق لا يفتح

تحقق من متغيرات التخزين، ومن صلاحيات الرابط، ومن أن خدمة التخزين الخارجية تعمل. يجب أن تكون قاعدة البيانات تحفظ مرجع الملف فقط، وليس محتوى الملف نفسه.

## أفضل اختيار لك

بالنسبة للنسخة الحالية من وصول، استخدم **Railway** أولًا. هو الأنسب لتشغيل الخادم وقاعدة البيانات معًا بطريقة مباشرة. استخدم Render إذا أردت بديلًا يعتمد على Web Service متصل بمستودع GitHub.

### مراجع

[1]: https://render.com/docs/deploy-node-express-app "Render: Deploy a Node Express App"
[2]: https://docs.railway.com/guides/express "Railway: Deploy an Express App"
