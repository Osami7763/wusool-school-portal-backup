# دليل نشر منصة وصول من جهازك

## الخلاصة المهمة قبل البدء

منصة وصول الحالية ليست صفحة HTML ثابتة فقط. هي نظام كامل يحتوي على واجهة React، وخادم Node.js، وقاعدة بيانات مشتركة، وتخزين للملفات والمرفقات. لذلك فإن رفع ملفات الواجهة إلى Vercel وحدها لن يحافظ على الإضافة والتعديل والمزامنة بين الأجهزة.

حتى تعمل المنصة بكامل وظائفها على Vercel، يجب أن يكون النشر شاملًا للواجهة والخادم وقاعدة البيانات والتخزين ومتغيرات البيئة. إذا نُشرت الواجهة فقط، فقد تظهر صفحة الطلاب، لكن لن تعمل الإدارة والمزامنة ورفع الملفات بالشكل الصحيح.

## ما تحتاجه

تحتاج إلى جهاز متصل بالإنترنت، وحساب GitHub، وحساب Vercel، وNode.js، ومدير الحزم pnpm. يفضّل استخدام كمبيوتر، لأن تجهيز مشروع كامل على الجوال أصعب بكثير.

ستحتاج أيضًا إلى بيانات الخدمات التي يعتمد عليها المشروع، مثل رابط قاعدة البيانات، ومفتاح الجلسات، وبيانات تخزين الملفات. لا تضع هذه البيانات داخل ملفات الواجهة، ولا ترسلها في المحادثات، ولا ترفعها إلى GitHub.

## أولًا: تنزيل النسخة الصحيحة

نزّل ملف المشروع المرفق مع هذا الدليل، ثم فك الضغط في مكان واضح على جهازك. افتح المجلد الذي يحتوي على `package.json`، وليس مجلد `client` وحده.

قبل رفع المشروع إلى GitHub، احذف أي ملف إعداد محلي يحتوي على أسرار أو بيانات اتصال. إذا وجدت ملفًا باسم `.project-config.json` أو أي ملف يبدأ اسمه بـ`.env`، فلا ترفعه إلى GitHub ولا إلى Vercel. هذه الملفات قد تحتوي على كلمات مرور ومفاتيح خدمات.

استخدم نسخة المشروع الآمنة فقط عند الرفع. إذا كانت لديك نسخة قديمة تحتوي على بيانات سرية، فلا تشاركها مع أي شخص، وغيّر المفاتيح التي ظهرت فيها.

## ثانيًا: تثبيت الأدوات على الكمبيوتر

ثبّت Node.js من الموقع الرسمي، ثم افتح PowerShell في Windows أو Terminal في macOS وLinux. تحقق من التثبيت بكتابة:

```text
node --version
npm --version
```

ثبّت pnpm بالأمر التالي:

```text
npm install --global pnpm
```

بعد ذلك ادخل إلى مجلد المشروع، ثم ثبّت المكتبات:

```text
cd مسار-مجلد-وصول
pnpm install
```

إذا كنت تستخدم Windows، يمكنك كتابة `cd` ثم سحب مجلد المشروع إلى نافذة PowerShell ليظهر المسار تلقائيًا.

## ثالثًا: اختبار المشروع على جهازك

شغّل فحص المشروع والاختبارات والبناء:

```text
pnpm check
pnpm test
pnpm build
```

إذا نجحت الأوامر، شغّل الموقع محليًا:

```text
pnpm dev
```

ستظهر لك وصلة محلية في الطرفية. افتحها في المتصفح. جرّب صفحة الطلاب، ثم افتح صفحة الإدارة وسجّل الدخول، وأضف عنصرًا تجريبيًا، وتأكد من ظهوره في صفحة الطلاب.

بعد انتهاء الاختبار أوقف الخادم بالضغط على `Ctrl + C`.

## رابعًا: رفع المشروع إلى GitHub

أنشئ مستودعًا جديدًا في GitHub واجعله خاصًا إذا كان المشروع سيستخدم داخل المدرسة فقط.

من داخل مجلد المشروع نفّذ الأوامر التالية بعد تغيير الرابط إلى رابط مستودعك:

```text
git init
git add .
git commit -m "Initial Wusool school portal"
git branch -M main
git remote add origin رابط-مستودع-GitHub
git push -u origin main
```

تأكد من أن المستودع لا يحتوي على أي كلمة مرور أو مفتاح API أو رابط قاعدة بيانات يتضمن كلمة مرور. راجع الملفات قبل تنفيذ `git push`.

## خامسًا: إنشاء مشروع في Vercel

ادخل إلى Vercel وسجّل الدخول باستخدام حساب GitHub. اختر **Add New Project** ثم اختر **Import Git Repository** وحدد مستودع وصول.

في إعدادات المشروع استخدم إعدادات البناء الموجودة في المشروع. اسم المشروع يمكن أن يكون `wusool-106` لأن النسخة الحالية مخصصة لشعبة 106.

لكن انتبه: المشروع الحالي يستخدم خادم Node.js طويل التشغيل، بينما Vercel يعتمد عادةً على وظائف الخادم وملفات API. لذلك قد ينجح بناء الواجهة، لكن قد لا يعمل الخادم الكامل بمجرد الضغط على Deploy. إذا ظهر الموقع بدون الإدارة أو بدون المزامنة، فهذا يعني أن خادم Node.js لم يتم تحويله إلى بنية Vercel المناسبة.

## سادسًا: متطلبات تشغيل النسخة الكاملة على Vercel

للحفاظ على كل وظائف وصول، يجب تجهيز نسخة Vercel كاملة قبل النشر النهائي. تشمل التجهيزات المطلوبة تحويل نقطة تشغيل الخادم إلى صيغة API مناسبة لـVercel، وضبط مسارات طلبات tRPC، وضبط إعادة توجيه الطلبات، وربط قاعدة البيانات الخارجية، وربط تخزين الملفات، واختبار الجلسات ورفع الملفات.

هذه الخطوة ليست إعدادًا عاديًا من لوحة Vercel فقط. هي تعديل هندسي في طريقة تشغيل الخادم. لا تعتمد على نشر الواجهة فقط إذا كان المطلوب هو الإضافة والمزامنة بين جميع الأجهزة.

## سابعًا: إضافة متغيرات البيئة في Vercel

من لوحة مشروع Vercel افتح:

**Settings → Environment Variables**

أضف المتغيرات المطلوبة من إعدادات مشروعك. أسماء المتغيرات الأساسية هي:

- `DATABASE_URL`
- `JWT_SECRET`
- `BUILT_IN_FORGE_API_URL`
- `BUILT_IN_FORGE_API_KEY`
- `VITE_FRONTEND_FORGE_API_URL`
- `VITE_FRONTEND_FORGE_API_KEY`
- `VITE_OAUTH_PORTAL_URL`
- `VITE_APP_ID`
- `OAUTH_SERVER_URL`
- `OWNER_OPEN_ID`
- `OWNER_NAME`

استخدم القيم الحقيقية من إعدادات الخدمات الخاصة بك. لا تستخدم قيمًا تجريبية، ولا تضع هذه القيم في ملفات JavaScript أو TypeScript، ولا تشاركها في لقطات الشاشة.

فعّل المتغيرات للبيئات التي تحتاجها. عادةً تحتاج إلى **Production** و**Preview** أثناء الاختبار. بعد إضافة المتغيرات، أعد النشر من تبويب **Deployments** باختيار **Redeploy**.

## ثامنًا: قاعدة البيانات

قاعدة البيانات يجب أن تكون خدمة MySQL أو TiDB متاحة عبر الإنترنت. يجب أن تسمح باتصال خادم Vercel بها، مع تفعيل التشفير عند الحاجة.

قبل النشر، تأكد من أن الجداول موجودة، ومنها جدول سجل التغييرات. لا تنشئ قاعدة بيانات جديدة فارغة إذا كنت تريد المحافظة على بيانات شعبة 106 الحالية.

إذا أنشأت قاعدة بيانات جديدة، ستحتاج إلى تشغيل migrations ثم إضافة المواد والمحتوى من لوحة الإدارة مرة أخرى. لا تنفذ أي عملية حذف أو إعادة تهيئة قبل أخذ نسخة احتياطية.

## تاسعًا: التخزين والملفات

المرفقات لا ينبغي أن تُحفظ داخل قاعدة البيانات نفسها. يجب أن تُحفظ في خدمة تخزين خارجية، وتُحفظ وصلة الملف واسم الملف في قاعدة البيانات.

بعد النشر، اختبر رفع ملف صغير من لوحة الإدارة، ثم افتح صفحة الطلاب من جهاز آخر. إذا ظهر اسم الملف دون إمكانية فتحه، فالمشكلة غالبًا في إعداد التخزين أو صلاحيات الوصول أو متغيرات البيئة.

## عاشرًا: اختبار النشر

بعد نجاح النشر، اختبر الوظائف بالترتيب التالي:

1. افتح صفحة الطلاب من نافذة خاصة.
2. اكتب اسم طالب تجريبي.
3. افتح صفحة الإدارة.
4. سجّل الدخول بالبيانات التي حددتها.
5. أضف واجبًا تجريبيًا لشعبة 106.
6. افتح صفحة الطلاب من جهاز أو متصفح آخر.
7. تأكد من ظهور الواجب.
8. أضف ملفًا ورابطًا تجريبيًا.
9. عدّل المورد ثم احذفه.
10. افتح سجل التغييرات وتأكد من ظهور اسم المدير والتاريخ والعملية.
11. جرّب الوضع الداكن والفاتح.
12. جرّب الجوال والكمبيوتر.

احذف البيانات التجريبية بعد انتهاء الاختبار، ولا تحذف أي بيانات فعلية دون التأكد من النسخة الاحتياطية.

## إذا فشل نشر Vercel

إذا ظهر خطأ في البناء، افتح تبويب **Deployments** ثم اختر عملية النشر الفاشلة واقرأ **Build Logs**. أكثر الأسباب احتمالًا هي عدم وجود متغيرات البيئة، أو استخدام أمر بناء غير مناسب، أو أن خادم Node.js الحالي يحتاج إلى تحويل لطريقة تشغيل Vercel.

إذا ظهرت الواجهة ولكن لم تعمل الإدارة أو لم تتزامن البيانات، فهذا ليس خطأ في صفحة الطلاب فقط. معناه أن جزء الخادم أو قاعدة البيانات لم يُنشر أو لم تُربط متغيراته بشكل صحيح.

في هذه الحالة لا تنشر نسخة ناقصة للعامة. استخدم النسخة المنشورة عبر WebDev إلى أن تُجهز نسخة Vercel الكاملة، أو اطلب تعديل المشروع ليتوافق مع بنية Vercel قبل إعادة النشر.

## الفرق بين رابط المعاينة والعلامة المائية

علامة Manus التي قد تظهر في بعض لقطات المعاينة ليست علامة مائية داخل تطبيق وصول المنشور. عند فتح الموقع النهائي من رابطه، لا تكون هذه العلامة جزءًا من واجهة الموقع.

## الخطة المناسبة لمشروع 106

ابدأ بالنشر التجريبي لشعبة 106 فقط. لا تعمم الرابط على المدرسة كلها قبل التأكد من أن البيانات لا تختلط بين الطلاب، وأن الملفات لا تكون مكشوفة، وأن الدخول إلى الإدارة يعمل من جهاز مستقل.

بعد نجاح التجربة، يمكن تطوير النظام ليشمل الصفوف والشعب الأخرى مع فصل بيانات كل شعبة عن الأخرى. في تلك المرحلة يجب إضافة اختيار الصف والشعبة، وحسابات للمعلمين والطلاب، وصلاحيات مختلفة، ولوحة إدارة مركزية للمدرسة.

## الخلاصة

إذا أردت تشغيل النسخة الكاملة الحالية بسرعة، فالنشر الموجود عبر WebDev هو المسار الجاهز. أما Vercel فيحتاج إلى تجهيز إضافي للخادم وقاعدة البيانات والتخزين. لا ترفع المشروع كصفحة ثابتة إذا كان هدفك هو استمرار الإضافة والمزامنة بين الأجهزة.

### مراجع

[1]: https://vercel.com/docs "Vercel Documentation"
[2]: https://docs.github.com/en/repositories/creating-and-managing-repositories "GitHub Repository Documentation"
[3]: https://nodejs.org/en/download "Node.js Downloads"
